<h3>Discover</h3>
<ul class="list-group list-group-flush">
  <% @stories.each do |story| %>
    <li class="list-group-item">
      <%= link_to story.title, story_path(story.code)%> <small class="text-muted">by <%= story.author.username %></small>
    </li>
  <% end %>
</ul>
