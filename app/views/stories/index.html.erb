<ul class="list-group list-group-flush">
  <% @stories.each do |story| %>
    <li class="list-group-item">
      <%= link_to story.title, story_path(story.code)%>
    </li>
  <% end %>
  <li class="list-group-item"><strong><%= link_to("new story", new_story_path) %></strong></li>
</ul>
